var totalCount = 14;
function ChangeBackground(assets_path){
    var num = Math.ceil( Math.random() * totalCount );
    $("body").css("background-image", "url('" + assets_path + '/bgtexture/' + num + '.png' + "')");
    $("body").css("background-repeat","repeat");
}