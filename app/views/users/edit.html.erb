<% provide(:title, "Edit user") %>
<h1>Edit</h1>

<div class="row">
  <div class="span4 well">
    <div class="legend">Update your profile</div>
    <%= form_for(@user) do |f| %>
        <%= render 'fields', f: f %>

        <%= f.submit "Save changes", class: "btn-info btn" %>
    <% end %>

  </div>
</div>