 <header>
   <div class="navbar navbar-inverse navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
        <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to "Site Builder" , root_path, id: "logo", class: "brand" %>
        <div class="nav-collapse collapse">
          <ul class="nav">
            <li class="<%= nav_aktiv(root_path) %>" ><%= link_to "Home", root_path %></li>
            <li class="<%= nav_aktiv(help_path) %>"><%= link_to "Help", help_path %></li>

            <li class="<%= nav_aktiv(root_path) %>" ><%= link_to "Home", root_path %></li>
            <li class="<%= nav_aktiv(help_path) %>"><%= link_to "Help", help_path %></li>
            <li class="<%= nav_aktiv(root_path) %>" ><%= link_to "Home", root_path %></li>
            <li class="<%= nav_aktiv(help_path) %>"><%= link_to "Help", help_path %></li>

            <% if signed_in? %>
                <li class="<%= nav_aktiv(users_path) %>"><%= link_to "Users", users_path %></li>
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                    Account <b class="caret"></b>
                  </a>
                  <ul class="dropdown-menu">
                    <li class="<%= nav_aktiv(current_user) %>"><%= link_to "Profile", current_user %></li>
                    <li class="<%= nav_aktiv(edit_user_path(current_user)) %>"><%= link_to "Settings", edit_user_path(current_user) %></li>
                    <li class="divider"></li>
                    <li>
                      <%= link_to "Sign out", signout_path, method: "delete" %>
                    </li>
                  </ul>
                </li>
            <% else %>
                <li class="<%= nav_aktiv(signin_path) %>"><%= link_to "Sign in", signin_path %></li>
            <% end %>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </div>
  </div>
</header>